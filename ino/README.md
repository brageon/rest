# Matrix reasoning

Ranger: chunks files to folder nuvi. Rest inside ranger transform text folder nuvi to numerical gui. Pattern2 searches all ordinals based on integers from NLP variables. It is an early adoption before moving to elasticsearch. Sadly this was before word chunks inside oanc. This mean all ordinals here correspond to machine chars like \s from range.

Table: Ordinal index from trapezoid integral function with rms-index. Number of gaps between those ordinals (find-gaps). Take the range of ints inside matrix to combine. Helper to use combinatorics (frequency) or brute inside *chaos*. Optima for cross testing hypothesis (chaos/diffevol) with frequency. Kde for visualization in conjunction with svm.

Chaos: Zipf distribution. Brute with line-count to enforce a solution from diffevol. Csv-list to take words from Enron to eno.txt. Mesh: Numerical from symmetry to folder with matrix. Words to be used with stops, dell* to extract uniqe word list. Google News from Kaggle is too large to download. Metrics with row-sums, eigen, mean. 

Colab: Color plot from matrix folder with meshgrid. Overlay to visualize minimax ints in different m.txt files. Unit for using youn to identify text range. Riemann Zeta is a color plot of complex logarithms with primes. Z-score is agnostic of datasets.

Conceptual analysis: Z=GAI, J=AWM, G=NVI, A=CPI, T=QRI. Noun is general category. Verb is non-verbal. Interjections is cardinals. Pronoun is active category defined by ownership and it can be seen as boson. SVT formula (m/s) compared to RGB. Red is word count (distance), blue is word metric (time), green is trigram debt (velocity). 

In short frequency give us trigram magnitude of Firebird by Collin Mason. *Youn* were then tested with Stephen Hicks´ arguments for liberalism. I formulated most used letters like "Z" from V, S, L in Swedish debate from 2018. KD was too frank. M, SD not identified. Back then I did not know how to code. Therefore I used Canterburry Puzzles to describe logical laws (e.g. Leibniz lemma) inside arguments (e.g. masked man fallacy).